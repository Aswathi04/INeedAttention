// Not used yet — this gets filled in during Phase 2 (Master Doc Section 6).
// Leave empty for now. Phase 1 doesn't touch personalities at all.
export const personalityMap = {};
export const personalityPrompts = {};
